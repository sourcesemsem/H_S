services:
  - type: web
    plan: free
    name: render-joker
    env: python
    branch: render
    startCommand: ./build.sh
    buildCommand: python3 -m jepthon
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: true
    autoDeploy: true
